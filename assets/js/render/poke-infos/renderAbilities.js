export default function rendedrMovesSection(pokemon) {
  const section = document.createElement("section");
  section.classList.add("poke__section", "poke__section--moves");
  section.innerHTML = `
    
  `;

  return section;
}
